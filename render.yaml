services:
  - type: web
    name: dark-messenger
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
    plan: free
