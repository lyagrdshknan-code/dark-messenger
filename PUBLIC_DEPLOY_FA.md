DARK MESSENGER - PUBLIC INTERNET DEPLOYMENT

نکته اصلی:
این پروژه مثل یک سایت واقعی طراحی شده، اما خود ZIP به تنهایی سایت عمومی نیست.
برای اینکه مثل سایت‌های معمولی از هر اینترنتی باز شود، باید این پوشه روی یک سرور عمومی
قرار بگیرد و یک دامنه/آدرس HTTPS به آن وصل شود.

روی سرور:
1) Node.js 18 یا بالاتر نصب باشد.
2) این پوشه را آپلود کن.
3) CMD/Terminal داخل پوشه:
   npm install
   npm start
4) پورت 3000 را باز کن یا Reverse Proxy (Nginx/Caddy) را روی HTTPS تنظیم کن.

برای دامنه:
https://your-domain.example

برای شبکه داخلی:
http://SERVER-IP:3000

برای اینترنت عمومی:
کاربر نباید localhost یا IP خصوصی تو را بزند؛ باید دامنه/آدرس عمومی سرور را باز کند.

ذخیره:
data/messages.json = پیام‌ها
data/users.json = کاربران
uploads/ = فایل‌ها

برای سرویس‌هایی که فایل‌سیستم موقت دارند، persistent disk/object storage لازم است.
برای استفاده واقعی و چندکاربره، PostgreSQL/SQLite + object storage گزینه حرفه‌ای‌تر است.

ویس و Screen Share:
در مرورگرهای مدرن، برای دسترسی میکروفون و Screen Share روی اینترنت عمومی از HTTPS استفاده کن.
WebRTC برای شبکه‌های سخت ممکن است به TURN server نیاز داشته باشد.

محدودیت مهم:
هیچ برنامه‌ای نمی‌تواند تضمین کند «روی هر سیستم ایرانی» بدون توجه به ISP، فیلترینگ،
DNS، آنتی‌ویروس یا تنظیمات شبکه باز شود. سایت استاندارد و عمومی است؛ مسیر شبکه و محل
سرور روی دسترسی اثر دارد.
